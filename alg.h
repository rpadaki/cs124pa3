#include <vector>
#include <cstdint>
#include <algorithm>

using namespace std;

/*
*  Again, we implement all the
*  min-residue algorithms at once.
*
*/
uint64_t minResidue(vector<uint64_t>, int);